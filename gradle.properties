# Gradle daemon tuning
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# ---------------------------------------------------------------------------
# Toolchain pins. Every pin is evidence-based; see RECOVERY_NOTES.md:
#  * minecraft 1.21.11  - build target chosen by the user; the artifact's own
#                         intermediary surface is compatible with 1.21.9/10/11
#                         (verified symbol-by-symbol against FabricMC tables).
#  * yarn      1.21.11+build.6 - official FabricMC yarn mapping used for the
#                         mechanical intermediary->yarn rename (tools/remap_to_yarn.py).
#  * loader / fabric-api - latest stable versions publishing the exact API
#                         surfaces referenced by the decompiled sources.
#  * jna       5.19.1    - required by the vendored de.labystudio.spotifyapi
#                         Windows integration (com.sun.jna.*).
# ---------------------------------------------------------------------------
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.6
loader_version=0.19.3
fabric_version=0.141.6+1.21.11
modmenu_version=1.16.15
jna_version=5.19.1

# Mod coordinates as recorded in the artifact's fabric.mod.json (preserved verbatim)
mod_version=1.0.0
maven_group=com.automine
archives_base_name=autodao
